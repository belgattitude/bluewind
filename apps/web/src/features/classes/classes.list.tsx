import React, { useEffect, useState } from 'react';

type ClassesListProps = {
    handleEdit: (studentId: number) => void;
};

export const ClassesList: React.FC = props => {
    return (
        <div>
            <div>Classes list</div>
        </div>
    );
};
